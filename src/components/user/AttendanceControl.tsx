import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const AttendanceControl = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Controle de Presença</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Implementação do controle de presença em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};